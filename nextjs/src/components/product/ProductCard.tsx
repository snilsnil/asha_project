{
  /* <ul className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
  {products.map((product) => (
    <li
      key={product.productId}
      className="bg-neutral-900 rounded-lg overflow-hidden shadow hover:scale-105 transition-transform"
    >
      <img src={product.imageUrl} alt={product.productName} className="w-full h-64 object-cover" />
      <div className="p-4 flex justify-between items-center">
        <p className="text-white font-medium">{product.productName}</p>
        <span className="bg-blue-600 text-white text-xs px-3 py-1 rounded-full">$XX.XX</span>
      </div>
    </li>
  ))}
</ul> */
}
